apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: robo
  region: us-east-1

managedNodeGroups:
  - name: robo
    instanceTypes:
      - m5.large
      - c5.large
      - c4.large
    desiredCapacity: 3
    minSize: 2
    maxSize: 5
    spot: true